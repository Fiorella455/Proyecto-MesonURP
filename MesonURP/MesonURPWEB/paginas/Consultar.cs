using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace MesonURPWEB.paginas
{
    public class Consultar
    {
        /*
         protected void btnBuscar_Click(object sender, EventArgs e)
    {
        if (x == 1)
        {

            libroObj.Libro_id = txtidOrdenCompra.Text;
            if (libroNegObj.LeerUsuario(libroObj))
            {
                txtAutor.Text = libroObj.Autor;
                txtTitulo.Text = libroObj.Titulo;
            }
            if (libroObj.Estado == 100)
            {
                btnBuscar.Text = "Borrar";
                x = 0;
            }
            mjeBuscar(libroObj);
        }

        if (x == 0)
        {
            txtAutor.Text = "";
            txtID.Text = "";
            txtTitulo.Text = "";
        }
    }

    public void mjeBuscar(Libro L)
    {
        lblMje.ForeColor = L.Estado == 100 ? Color.Green : Color.Red;
        switch (L.Estado)
        {
            case 100: //Libro registrado
                lblMje.Text = "Libro [" + L.Libro_id + "] se encontro correctamente !!!";
                break;
            case 385:
                lblMje.Text = "No existe el libro";
                break;
            default:
                lblMje.Text = "===???===";
                break;
        }
    }
         
         
         
         
         */
    }
}